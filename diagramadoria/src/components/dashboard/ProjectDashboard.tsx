// Legacy placeholder: this component is no longer used.
// Keeping a minimal export to avoid compile issues while we deprecate/remove this file.
import React from 'react';

const ProjectDashboard: React.FC = () => {
  return null;
};

export default ProjectDashboard;